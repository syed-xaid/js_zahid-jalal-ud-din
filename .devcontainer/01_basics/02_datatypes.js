"use strict";//treat all the code as the newer version of JavaScript

//alert(3+3);//we are using node.js not  browser

console.log(3+3);

let name="zahid"
let age=25;
let isApproved=true;
let state;

console.table([name, age, isApproved, state])

// the below data types are premitive datatypes
// number=> 2 to power 53
//bigint
// string=>" any text"
// boolean => ture/false
// state;
//  null  => standAlone value {Representation of an empty value }
// undefined =>
// symbol => used for uniqueness 


//Objects

console.log(typeof name)